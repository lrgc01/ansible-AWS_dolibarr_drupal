---
- hosts: webservers
  gather_facts: "{{ gather_y_n }}"
  become: true
  remote_user: "ubuntu"
  vars_files:
   - ../secret/general.yml
   - ../secret/base.yml
   - ../secret/db_adm.yml
   - ../secret/drupal.yml
   - ../secret/bkprest.yml
   - ../secret/secret.yml
  roles:
   - Restore

- hosts: vanhack_test
  gather_facts: "{{ gather_y_n }}"
  become: true
  remote_user: "ubuntu"
  vars_files:
   - vars_files/general.yml
   - vars_files/base.yml
   - vars_files/db_adm.yml
   - vars_files/bkprest.yml
  roles:
   - Restore
